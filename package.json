{
  "name": "dsiohaandbold",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "hugo -s site -d ../build --cleanDestinationDir"
  },
  "devDependencies": {
    "hugo-bin": "^0.23.0",
    "rimraf": "^2.6.2"
  }
}
